"use client";
import { useRef } from "react";
import { gsap } from "gsap";

export default function SvgMaskScrollPage() {
  //  TODO: add lenis scroll here to make it smoother
  // use videos that are revealed with mask
  return (
    <div className={`bg-[#ddd] h-screen flex flex-col justify-between`}></div>
  );
}
